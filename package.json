{
  "name": "blogverse",
  "version": "1.0.0",
  "description": "```bash\r git clone https://github.com/huydevpmg/BlogVerse.git",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd backend && npm start",
    "build-fe": "cd BlogVerse-FE && npm install --production=false && npm run build",
    "build-be": "cd backend && npm install",
    "build": "npm run build-fe && npm run build-be"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
